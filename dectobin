#Python function to convert decimal numbers to binary strings using Stack

def dec2bin(dec):
    """
    This function converts a decimal number
    to its binary equivalent using a Stack
    input type: integer
    return type: string
    """
    #create an empty stack that will hold the remainder
    remStack = Stack()

    while dec > 0:
        rem = dec%2
        remStack.push(rem)
        dec = dec//2

    binString = ""

    while not remStack.isEmpty():
        binString = binString + str(remStack.pop())

    return binString
